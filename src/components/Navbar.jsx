import {useEffect,useState} from 'react';import{Menu,X}from'lucide-react';import{studio}from'../data/weddingData';
const links=[['Portfolio','portfolio'],['About','about'],['Packages','packages'],['Contact','contact']];
export default function Navbar(){const[open,setOpen]=useState(false),[scrolled,setScrolled]=useState(false);useEffect(()=>{const f=()=>setScrolled(scrollY>30);addEventListener('scroll',f);return()=>removeEventListener('scroll',f)},[]);return <header className={`fixed inset-x-0 top-0 z-50 transition-all duration-500 ${scrolled||open?'bg-ivory/95 py-3 shadow-sm backdrop-blur':'bg-transparent py-5 text-white'}`}><nav className="container-pad flex items-center justify-between" aria-label="Primary navigation"><a href="#home" className="font-serif text-2xl tracking-wide">{studio.name}</a><div className="hidden items-center gap-8 md:flex">{links.map(([n,id])=><a key={id} className="text-[10px] font-medium uppercase tracking-[.18em] transition hover:text-champagne" href={`#${id}`}>{n}</a>)}</div><a href="#contact" className={`hidden rounded-full border px-5 py-2.5 text-[10px] font-semibold uppercase tracking-[.16em] transition lg:inline-flex ${scrolled?'border-ink/25 hover:bg-ink hover:text-white':'border-white/45 hover:bg-white hover:text-ink'}`}>Enquire</a><button onClick={()=>setOpen(!open)} className="grid size-11 place-items-center md:hidden" aria-label="Toggle navigation" aria-expanded={open}>{open?<X/>:<Menu/>}</button></nav>{open&&<div className="container-pad flex flex-col gap-5 pb-5 pt-4 md:hidden">{links.map(([n,id])=><a key={id} onClick={()=>setOpen(false)} className="text-sm uppercase tracking-[.16em]" href={`#${id}`}>{n}</a>)}</div>}</header>}
